Use the **encode-repo** skill to encode the repository at `{{REPO_PATH}}`.

Inputs:
- Repo path: `{{REPO_PATH}}`
- Profile: `{{PROFILE}}`
- Project name: `{{PROJECT_NAME}}`
- Active project id: `{{PROJECT_ID}}`

Confirm these inputs back in one line, then proceed through the phases as the skill describes. After Phase 7B's final summary, stop — do not continue with unrelated work.
